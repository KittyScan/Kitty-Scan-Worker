# Dependencies
node_modules/

# Build / Wrangler
dist/
.wrangler/

# Secrets — never commit. Stored as Cloudflare Worker secrets via `wrangler secret put`.
.dev.vars
.env
.env.local
*.p8
*.pem
secrets/

# Logs
*.log
npm-debug.log*

# macOS
.DS_Store

# Eval images, per-run reports, and analyze-response cache — local-only,
# not source. Test case JSON specs in evals/cases/ are tracked normally.
evals/images/
evals/reports/
evals/.cache/

# Editor
.vscode/
.idea/
*.swp
