# Dependencies
node_modules/

# Build / Wrangler
dist/
.wrangler/

# Secrets — never commit. Stored as Cloudflare Worker secrets via `wrangler secret put`.
.dev.vars
.env
.env.local
*.p8
*.pem
secrets/

# Logs
*.log
npm-debug.log*

# macOS
.DS_Store

# Eval images and per-run reports — local-only, big binaries, not source.
# Test case JSON specs in evals/cases/ are tracked normally.
evals/images/
evals/reports/

# Editor
.vscode/
.idea/
*.swp
