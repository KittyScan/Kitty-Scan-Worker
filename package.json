{
  "name": "carmel-worker",
  "version": "0.1.0",
  "description": "Carmel cat-health API proxy (Anthropic Claude behind a rate-limited Cloudflare Worker)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.84.1"
  }
}
